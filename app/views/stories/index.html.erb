<h2>Narrative</h2>

<% @stories.each do |story| %>

  <% @parts = Part.where(story_id: story.id) %>

  <% if @parts.length == 10 %>
  <div>
    <a href="/stories/<%= story.id %>"><h3><%= story.title %>STORY TITLE</h3></a>
    <% @parts.each do |part| %>
      <%= part.body %>
    <% end %>
  </div>
  <% end %>

<% end %>


<h2>Fragments</h2>

<% @stories.each do |story| %>

  <% @parts = Part.where(story_id: story.id) %>

  <% if @parts.length < 10 %>
  <div>
    <a href="/stories/<%= story.id %>"><h3><%= story.title %>STORY TITLE</h3></a>
    <% @parts.each do |part| %>
      <%= part.body %>
    <% end %>
  </div>
  <% end %>

<% end %>
